// PR2a の placeholder。Vite + React build pipeline 動作確認用の空 SPA。
// PR3a で SignIn / SignUp / Error / VerifyMagicLink の 4 画面に置き換える。

export const App = () => {
  return (
    <main>
      <h1>taimei-auth Layer B</h1>
      <p>Vite build pipeline placeholder (PR2a)</p>
    </main>
  );
};
