# PoC (候補 A、M5): oven/bun image の node は bun への fallback shim で wrangler が拒否するため、本物の Node.js を重ねる。
ARG APP_ENV=development
FROM node:22-bookworm-slim AS node-bin

FROM taimei-auth-dev AS wrangler-dev
COPY --from=node-bin /usr/local/bin/node /usr/local/bin/node
