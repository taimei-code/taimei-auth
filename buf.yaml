version: v2
modules:
  - path: proto
breaking:
  use:
    - WIRE
  # reserved field の再利用を許可している。再利用は予約時と同名・同型の時だけ wire 互換 (旧 client は unknown field として読み飛ばす)
  except:
    - RESERVED_MESSAGE_NO_DELETE
